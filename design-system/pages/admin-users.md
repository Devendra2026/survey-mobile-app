# Admin Users page overrides

- Surface header (not brand) with embedded search
- Segmented role filters (rounded-full pills)
- User cards: avatar left, role tag top-right, tenancy tags below
- Card radius `rounded-2xl`, subtle shadow
