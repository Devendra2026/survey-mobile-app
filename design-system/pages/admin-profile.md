# Admin Profile page overrides

- Brand gradient hero with centered avatar
- Overlapping stat tiles (pending, active users) with tap-through
- Grouped navigation card with dividers
- Sign out: outline button at bottom
