/**
 * Convex dev is owned by the monorepo backend (canonical backend).
 * Run from ../sdv-monorepo-apps/packages/backend: `npm run dev`
 */
console.error(
  '[convex dev] Convex backend lives in ../sdv-monorepo-apps/packages/backend.\n' +
    '  cd ../sdv-monorepo-apps/packages/backend && npm run dev\n' +
    '  Then refresh mobile types: npm run sync:convex\n',
);
process.exit(1);
